# Rejected round-robin blank-trade extraction

> The user explicitly rejected **另一半** on 2026-08-29.  The allocation of
> each fill's two characters to two numbered partners and the subsequent
> frequency ranking were not stated by the puzzle.  This file is retained as
> negative evidence and must not be used to restore that candidate.  The
> central depth clue is now filled as `END`, not the former arbitrarily
> grouped `BREAK`; this removes another premise of the construction below.

This artifact records the former extraction from the three travelling blank
fills after all 57 geometric three-Han-block trades repaired the clues.

## Hint 3 and the depth axis

Centering the three printed boards in one 13×13 frame leaves exactly five
coordinates open in all three layers.  Hint 3 says that the three cell
contents at each such coordinate form the dimension absent from an ordinary
crossword: the depth/`里` direction.  In G1→G2→G3 order the restored depth
rows are:

| Centered coordinate | Depth line | Three cell contents | Repaired clue |
| --- | --- | --- | --- |
| r04c04 | Z01 | `一个可 / 有可无 / 的影子` | 一个可有可无的影子 |
| r04c10 | Z02 | `学狐狸 / 能达成 / 这件事` | 学狐狸能达成这件事 |
| r07c07 | Z03 | `结束本 / 部分的 / 关键词` | 结束本部分的关键词 |
| r10c04 | Z04 | `两根骨 / 头连接 / 根与尖` | 两根骨头连接根与尖 |
| r10c10 | Z05 | `记忆中 / 厕所里 / 的味道` | 记忆中厕所里的味道 |

These 15 plane/depth crossings combine with 42 planar crossings to give the
57 trades.  This validates the clue-block geometry; it does not validate the
separate, rejected whole-word crossword fill formerly used for 中山大学.

## Inputs and instructions

The three blank fills, in their source-statement order, are:

```text
B1 = 榜样
B2 = 曼丽
B3 = 气性
```

The unique maximum-frequency three-Han block in the repaired clues is
`关键词`, occurring twice.  The old route treated its two clue answers as
`反切` and `BREAK`; the second is correctly `END`, so using it to “break” the
blank fills was unsupported in addition to the arbitrary round-robin rule.

## Three-way round-robin trade

There are three labelled participants.  Each participant has exactly two
characters and exactly two other participants, so assign its characters
left-to-right to the other participant labels in ascending order:

| Participant | Character for earlier partner | Character for later partner |
| --- | --- | --- |
| B1 `榜样` | B2 receives `榜` | B3 receives `样` |
| B2 `曼丽` | B1 receives `曼` | B3 receives `丽` |
| B3 `气性` | B1 receives `气` | B2 receives `性` |

Pairing the mutual allocations gives every bilateral trade exactly once:

| Trade | Pair |
| --- | --- |
| B1–B2 | `榜曼` |
| B1–B3 | `样气` |
| B2–B3 | `丽性` |

## High-frequency order and fanqie

Count the two characters of each trade in all 50 repaired clue texts after
filling the blanks, and rank by the summed frequency:

| Rank | Trade | Character frequencies | Sum | Fanqie |
| ---: | --- | --- | ---: | --- |
| 1 | `丽性` | `丽×3 + 性×3` | 6 | `ling4` |
| 2 | `样气` | `样×2 + 气×1` | 3 | `yi4` |
| 3 | `榜曼` | `榜×1 + 曼×1` | 2 | `ban4` |

Ignoring tone in the phonetic landing gives:

```text
ling / yi / ban  ->  另 / 一 / 半  ->  另一半
```

The same strict order is already present before the three blanks are filled:
the corresponding raw frequency sums are `4 > 1 > 0`.

## Bounded controls and disambiguation

- Reversing any subset of the three original two-character fills gives eight
  orientations.  Applying the identical allocation, frequency, and fanqie
  rules to all eight produces a three-Han dictionary landing only for the
  unreversed source orientation.
- That orientation has two toneless local-dictionary homophones: `另一半` and
  `另一版`.  The Round is explicitly about love, and `另一半` is the ordinary
  relationship term; `另一版` has no comparable thematic fit.
- The middle carrier gives `yi4`, whereas `一` is normally `yi1`.  The clue
  pointing to the earlier 河海解谜 puzzle specifically supplies a fanqie
  convention in which tones may be changed, so the segmental reading is the
  intended signal.

The full eight-orientation output is in `work/blank_trade_graph.md` and is
generated by `work/blank_trade_graph.py`.
