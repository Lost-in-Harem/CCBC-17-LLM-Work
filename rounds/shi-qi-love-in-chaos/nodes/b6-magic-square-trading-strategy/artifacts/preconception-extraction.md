# Rejected extraction: PRECONCEPTION

**Verdict (2026-08-31): rejected by the user.** The construction
`古代→PRE` plus `概念→CONCEPTION` is therefore not the puzzle's final step;
the related `PERCEPTION / PERCEPT` prefix-trade family was already non-unique.

## 1. Reconstruct and fill the three-dimensional crossword

Undoing the exchange of three-Han blocks at all 42 planar and 15 depth
crossings restores 50 natural clues.  Their answers decompose into ordered
visible components of the required entry lengths.  Counting a component only
within its stated direction (`横 / 纵 / 里`) gives a complete numeric fill in
which all 57 crossings agree.

This establishes both the traded-block map and the five depth entries; it is
not an optional semantic reconstruction.

## 2. Read the literal highest-frequency trades

The 50 repaired clues contain 180 three-Han block occurrences and 179 distinct
texts.  The unique maximum-frequency block is `关键词`, occurring twice.  Its
two actual trading partners, in repaired-host statement order, are:

```text
古代人 / 的概念
```

The two blocks concatenate without alteration:

```text
古代人的概念
```

The two repaired hosts answer `反切` and `END`; the latter marks this as the
end of the extraction.  No company name, compass projection, four-corner
square, or second homophone stream is required.

## 3. Resolve the English construction

Treat the final phrase as a compact English word-construction clue:

```text
古代、在前       -> PRE-
（人的）概念     -> CONCEPTION
PRE + CONCEPTION -> PRECONCEPTION
```

`PRECONCEPTION` means a notion or view formed beforehand.  A bounded local
control over common alternative glosses found this to be the only lexical
concatenation among `PRE/PALEO/ARCHAEO` and
`CONCEPT/CONCEPTION/IDEA/NOTION`.  Trying to trade prefixes of `PERSON` and
`CONCEPT(ION)` instead is non-unique (`PERCEPT / PERCEPTION`) and discards
`古代`.

## 4. Read-only Meta check

The accepted Round Meta pairs this feeder with `观音菩萨`: `观` is compatible
with a preconceived view.  Left-aligning the suffix `PUSA` with the candidate
gives exactly one same-position match:

```text
PUSA
PRECONCEPTION
P
```

This reproduces the accepted Meta answer's first letter.  It is a check on the
locally derived word, not the source of the candidate.

## Reproduction pointers

- `artifacts/all-crossing-reconstruction.md` records the 57 block exchanges.
- `artifacts/direction-frequency-fill.md` records the component-frequency
  crossword.
- `artifacts/high-frequency-extraction.md` identifies both `关键词` trades and
  their partners.
- `work/english-prefix-trade-audit.md` records the bounded English landing
  comparison.

No online answer or official solution was consulted.
